<!DOCTYPE html>
html(lang="en")

    head

        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")

        link(rel="stylesheet", href="Assets/Scss/style.css")

        title RPS | Multiplayer

    body.pad

        // Real time update method

        //#output Hotdog Status:

        //input(type="text", id="userInput")

        //button(type="submit" id="save") Save

        // ----------------------------------------------------------------- //

        #top-bar-container

            #title ROCK | PAPER | SCISSORS

            input#txtEmail(type="email", name="userEmail" placeholder="Email")

            input#txtPassword(type="password", name="userPassword" placeholder="Password")

            button#btnLogin Login

            button#btnSignUp Sign up

            button#btnLogout(style="display: none;") Logout

            #user-display-name Default

        #main-divider

        #content-container

            #player-area

                #player-1-container.player.pad

                    #player-1-name.player-name

                    .player-name-info-divider

                    #player-1-info.player-info

                .player-divider

                #results-display-container.pad

                .player-divider

                #player-2-container.player.pad

                    #player-2-name.player-name

                    .player-name-info-divider

                    #player-2-info.player-info

            #content-divider

            #player-chat-container

                #chat-display

                #message-container

                    input#chat-input(type="text")

                    button#chat-send-message Send

        // Only needed Firebase features

        script(src="https://www.gstatic.com/firebasejs/4.10.1/firebase.js")

        //- script(src="https://www.gstatic.com/firebasejs/4.10.1/firebase-app.js")
        //- script(src="https://www.gstatic.com/firebasejs/4.10.1/firebase-auth.js")
        //- script(src="https://www.gstatic.com/firebasejs/4.10.1/firebase-firestore.js")
        //- script(src="https://www.gstatic.com/firebasejs/4.10.1/firebase-messaging.js")

        script(src="Assets/Src/app.js")